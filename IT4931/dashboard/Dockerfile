FROM node:20-alpine AS development

WORKDIR /app

# We assume a standard package.json will be created
# For now, this is a placeholder to make docker-compose valid
# COPY package*.json ./
# RUN npm install

# COPY . .

EXPOSE 5173

# CMD ["npm", "run", "dev", "--", "--host"]
CMD ["sh", "-c", "echo 'Dashboard placeholder' && sleep infinity"]
